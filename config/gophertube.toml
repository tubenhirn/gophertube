# GopherTube Configuration File
# Copy this file to ~/.config/gophertube/gophertube.toml and customize as needed

# Number of search results to fetch
# Default: 30 (lower it to increase speed)
search_limit = 30 
# Default video quality (e.g. 1080p, 720p, 480p, 360p)
quality = "1080p" 
# Path to save downloaded videos (e.g. /home/user/Videos/GopherTube)
downloads_path = "/home/$USER/Videos/GopherTube" 
# Default theme (e.g. Minimal, Gopher, Gruvbox)
theme = "Minimal"
# Start mpv in fullscreen for video playback (default: true).
# Set to false to play in a windowed mpv (e.g. when using a tiling window manager).
fullscreen = true
# Use the Kitty graphics protocol for thumbnail previews (default: false).
# Only enable this in terminals that support it (e.g. Kitty, WezTerm, Ghostty).
# When false, chafa falls back to Unicode block symbols, which work in any terminal.
kitty_graphics = false
